# Route/Corridor Acceptance

- Both symbols compile and register independently.
- Node tests cover two-point, curved, width, topology, invalid and PlotJSON cases.
- Playground exposes both symbols in production and under an explicit E2E flag.
- Browser tests verify draft and committed rendered features for both.
- One implementation PR completes the group.
